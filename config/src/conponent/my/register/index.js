import React,{Component} from 'react';import './index.less';export default class Register extends Component{    constructor(){        super();        this.state={username:'',password:'',index:null};    }    btn=()=>{        let sigin={username:this.state.username,password:this.state.password};        let sigins=localStorage.getItem('sigin');        if(sigin.username.length<6&&sigin.password.length<6){            alert('帐号或密码太短，请重试')        }else{            if(sigins){                sigins=JSON.parse(sigins);                if(sigins.find(item=>item.username==sigin.username)){                    alert('用户名已经被注册了！！！！！请重新输入！！！！！')                }else{                    sigins.push(sigin);                    sigins=JSON.stringify(sigins);                    localStorage.setItem('sigin',sigins);                }            }else{                let ary=new Array(sigin);                localStorage.setItem('sigin',JSON.stringify(ary))            }        }    };    render(){        return (            <div className="sigins">                <div className="sigin">                    <li>                        <i className="iconfont icon-unie610">                        </i>                        <input                            placeholder="请输入您要注册的用户名"                            className="iphone"                            type="text"                            id="iphone"                            value={this.state.username}                            onChange={event=>this.setState({username:event.target.value})}/></li>                    <li>                        <i className="iconfont icon-password">                        </i>                        <input                            placeholder="请输入您要注册的密码"                            type="password"                            id="password"                            value={this.state.password}                            onChange={event=>this.setState({password:event.target.value})} />                    </li>                    <li><button onClick={this.btn}>注册</button></li>                </div>            </div>        )    }}